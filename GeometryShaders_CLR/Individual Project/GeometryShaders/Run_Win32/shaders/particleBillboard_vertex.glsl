#version 330 core

uniform mat4	gView;
uniform mat4	gProj;
uniform mat4	gModel;  

////////////////////////////////////////////////////////////////
//
in vec3			inPosition;
in vec4			inColor;
//
////////////////////////////////////////////////////////////////
//
out Vertex
{
	vec4 color;
}   vertex;
//
////////////////////////////////////////////////////////////////

void main()
{
	gl_Position = gView * gModel * vec4(inPosition, 1.0);
	vertex.color = vec4(1.0, 0.0, 0.0, 1.0);
}